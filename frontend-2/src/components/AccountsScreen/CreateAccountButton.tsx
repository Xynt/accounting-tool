const CreateAccountButton = () => {

  const addAccountClickHandler = () => {
    return true
  }

  return <button onClick={addAccountClickHandler}>Add Account</button>
}

export default CreateAccountButton